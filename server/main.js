import { Meteor } from 'meteor/meteor';

import "../imports/api/RemoteReq.js";
import "../imports/api/Users.js";
import "../imports/api/Historial.js";
import "../imports/api/Busquedas.js";

Meteor.startup(() => {
  // code to run on server at startup
});
